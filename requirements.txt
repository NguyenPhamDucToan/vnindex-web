# Export step only; the front-end needs no build deps.
# The valuation reuse imports from the sibling ../vnindex-valuation package,
# so run this in that project's virtualenv (which already has these).
pandas>=2.0
SQLAlchemy>=2.0
psycopg2-binary>=2.9
# export_commodities.py only (Yahoo Finance futures for the chart-21 branch).
yfinance>=0.2
